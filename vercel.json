{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd wod-tracker && npm install && npm run build",
  "outputDirectory": "wod-tracker/dist",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
